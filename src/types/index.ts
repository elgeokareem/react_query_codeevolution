export interface SuperHeroesType {
  id: number;
  name: string;
  alterEgo: string;
}

export interface Colors {
  id: number;
  label: string;
}

export interface AddSuperHero {
  name: string;
  alterEgo: string;
}
